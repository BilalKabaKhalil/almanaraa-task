import 'package:flutter/material.dart';

class ShippingCalculatorPage extends StatelessWidget {
  const ShippingCalculatorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('shipping calculator'),
    );
  }
}
